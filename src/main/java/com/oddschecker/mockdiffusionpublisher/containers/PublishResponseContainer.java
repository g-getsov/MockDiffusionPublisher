package com.oddschecker.mockdiffusionpublisher.containers;

import com.oddschecker.mockdiffusionpublisher.enums.ResponseStatus;

public class PublishResponseContainer extends BaseResponseContainer {

    public PublishResponseContainer(ResponseStatus status) {
        super(status);
    }
}
