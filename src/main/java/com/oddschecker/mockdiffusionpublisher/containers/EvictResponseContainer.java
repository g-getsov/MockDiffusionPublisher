package com.oddschecker.mockdiffusionpublisher.containers;

import com.oddschecker.mockdiffusionpublisher.enums.ResponseStatus;

public class EvictResponseContainer extends BaseResponseContainer {

    public EvictResponseContainer(ResponseStatus status) {
        super(status);
    }
}
